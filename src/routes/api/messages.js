import { Router } from "express";
import mongoose from "mongoose";

import messageSchema from "../../models/messageSchema";


const router = Router();

router.get("/messages/:room", (req, res, next) => {

});

router.put("/messages/:room", (req, res, next) => {

});